{
  "buildCommand": "cd frontend && bun install && bun run build && cd .. && bun install",
  "outputDirectory": "frontend/dist",
  "devCommand": "bun run dev",
  "framework": null,
  "rewrites": [
    {
      "source": "/((?!api/.*).*)",
      "destination": "/index.html"
    }
  ]
} 